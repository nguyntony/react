import React from 'react';
// this is similar to const React = require('react')

function HelloReact() {
    return (
        <div className="message">
            this component is created in HelloReact
        </div>
    );
}

export default HelloReact;
// this is similar to module.exports = HelloReact